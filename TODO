Pending features before a first release.

- DBus scanning is broken (endpoints are not found).
- Add all commands as of latest CXL Spec.
- Current testing is very simple, add more unit tests.
- More examples.

Others:
- Support vendor-specific commands.
- Rust (and other?) language bindings.